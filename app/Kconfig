menuconfig LED_SUBSYSTEM
	bool "LED Subsystem"
	default y
	choice
		prompt "Led blink sleep time"
		default BLINK_SLEEP_TIME_1000MS
		depends on LED_SUBSYSTEM
		config BLINK_SLEEP_TIME_250MS
			bool "Very Fast (250ms)"
		config BLINK_SLEEP_TIME_500MS
			bool "Fast (500ms)"
		config BLINK_SLEEP_TIME_1000MS
			bool "Medium (1s)"
		config BLINK_SLEEP_TIME_2000MS
			bool "Slow (2s)"
	endchoice
	config BLINK_SLEEP_TIME_MS
		int
		default 250 if BLINK_SLEEP_TIME_250MS
		default 500 if BLINK_SLEEP_TIME_500MS
		default 1000 if BLINK_SLEEP_TIME_1000MS
		default 2000 if BLINK_SLEEP_TIME_2000MS
		default 0
		depends on LED_SUBSYSTEM
	menuconfig ADV_LED_SETTINGS
		bool "Advanced LED settings"
		default n
		depends on LED_SUBSYSTEM
		config LED_BRIGHTNESS
			int "LED brightness (0-100)"
			default 100
			range 0 100
			depends on ADV_LED_SETTINGS
		config LED_FADE_DURATION
			int "LED fade duration (ms)"
			default 500
			range 0 5000
			depends on ADV_LED_SETTINGS
		menu "Expert settings"
			visible if ADV_LED_SETTINGS
			depends on ADV_LED_SETTINGS
			config LED_DEBUG
				bool "Enable LED debugging"
				default n
			config CUSTOM_BLINK_PATTERN
				bool "Custom blink pattern"
				default n
		endmenu

source "Kconfig.zephyr"
